import React from 'react';
import ReactDOM from 'react-dom/client';
import EndPointer from './components/EndPointer';

const root = ReactDOM.createRoot(
    document.getElementById('root') as HTMLElement
);
root.render(
    <React.StrictMode>
        <EndPointer />

    </React.StrictMode>
);
